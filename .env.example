# Supabase — sin usar todavía (ver src/store/repository/supabaseRepo.js).
# Copiar a .env cuando se conecte de verdad. Hasta entonces la app sigue
# corriendo 100% sobre localStorage, estas variables no se leen en ningún lado.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
